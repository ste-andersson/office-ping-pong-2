// "core" is temporarily labeled "Core & Alumni" for the AW party, since new
// players added via Arcade Mode land on this team. Revert to "SALT Core" here
// (and the matching <span> in index.html's player-picker-tabs) after the event.
export const TEAM_LABELS: Record<string, string> = {
  java: "Java",
  core: "Core & Alumni",
  "data-ai": "Data & AI",
};
